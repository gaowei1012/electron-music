import * as React from "react";
export default class Settings extends React.PureComponent {
  state = {};
  componentDidMount() {}
  render() {
    let renderSettings = <div>设置</div>;
    return <>{renderSettings}</>;
  }
}
